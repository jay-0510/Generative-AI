"""
utils package
-------------
Small, generic, reusable helper functions that don't belong to any one
agent -- currently just the safe SQL execution guard used by the SQL
agent path (see helpers.py).
"""
