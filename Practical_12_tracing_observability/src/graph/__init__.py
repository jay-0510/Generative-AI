"""
src/graph package
==================
The LangGraph "shape" of the agent: the shared state schema (state.py)
and the compiled StateGraph wiring nodes together (workflow.py). Node
*implementations* live in src/agents — this package only owns structure.
"""
