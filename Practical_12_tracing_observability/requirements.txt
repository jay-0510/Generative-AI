# These exact versions were installed together and verified against this
# project's test suite (all 9 tests passing) before this file was written --
# the LangChain ecosystem moves fast and moving parts (langchain-core,
# langchain, langgraph, langchain-aws, langchain-chroma) must stay in a
# mutually compatible major-version band, or `pip install` will fail with a
# dependency-resolution error. If you need to upgrade one, upgrade the
# whole group together and re-run `pytest tests/ -v` before trusting it.

# --- Core agent framework ---------------------------------------------------
langgraph==1.2.9
langchain-core==1.5.1
langchain==1.3.14          # only needed for langfuse.langchain.CallbackHandler's internal import
langchain-aws==1.6.3       # ChatBedrockConverse + BedrockEmbeddings (Amazon Nova / Titan)
boto3==1.43.57             # AWS SDK -- underlies langchain-aws's Bedrock calls

# --- RAG vector store --------------------------------------------------------
langchain-chroma==1.1.0
chromadb==1.5.9

# --- Config -------------------------------------------------------------------
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.0.1

# --- Observability (this practical's focus) -----------------------------------
langsmith==0.10.10         # LangSmith SDK/client (tracing + programmatic run analysis)
langfuse==4.14.1           # LangFuse SDK: @observe() decorator + LangChain CallbackHandler

# --- Notebook -------------------------------------------------------------------
notebook>=7.0
ipykernel>=6.29

# --- Tests ------------------------------------------------------------------------
pytest==8.3.4
